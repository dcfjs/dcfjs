{
  "name": "@dcfjs/workspace",
  "private": true,
  "workspaces": [
    "packages/**/*"
  ],
  "scripts": {
    "admin": "yarn workspace @df/admin-cli admin",
    "build": "lerna run build --stream",
    "build:proto": "lerna run build:proto --parallel --stream",
    "watch": "run-p watch:build watch:run watch:web",
    "watch:build": "lerna run watch:build --parallel --stream",
    "watch:run": "lerna run watch:run --parallel --stream",
    "watch:web": "lerna run watch:web --parallel --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "rollup": "^2.40.0",
    "rollup-plugin-banner": "^0.2.1",
    "rollup-plugin-clear": "^2.0.7",
    "rollup-plugin-typescript2": "^0.30.0",
    "typescript": "^4.2.3"
  }
}
